LocalHaos DEP Release Sync trigger.

Purpose:
- build next LocalHaos release
- publish APK release
- sync upstream ReVanced main after publication
- re-assert LocalHaos changes after sync

Included guarded features:
- online patch browser
- patch parser fixes
- Jman-Github ReVanced-Patch-Bundles raw JSON metadata support
- GitHub blob JSON to raw JSON normalization
- detected patch bundle visibility even when app-specific filter is empty
- isolated broken/empty patch bundle handling
- duplicate ReVanced patch display names allowed
- missing Morphe runtime class isolation
- installed app source
- unsplit APK bundle import
- split APK merge
- automatic system base APK source
- framework/config split filtering
- localhaos app update endpoint

Rerun reason:
- fixed Android versionCode parser for LocalHaos suffix versions such as 2.1.X-localhaos
- fixed AppArchiveImport KDoc Kotlin parse failure
- added guard that rejects parsed patch bundles with zero visible patches before UI display
- fixed DEP upstream sync to fetch only upstream/main with --no-tags and avoid upstream dev tag conflicts
- added Jman catalog generated patches-bundle.json parser support
- cleaned source display names and vnull rendering
- fixed patch detection so valid bundles remain detected even when no patches match the currently selected package
- fixed Source.kt syntax error caused by stray trailing brace
- fixed ArrayIndexOutOfBoundsException from empty/invalid patch bundle files by parsing each source independently
- fixed duplicate patch display names such as ReVanced 'Hide ads' being incorrectly rejected
- fixed ClassNotFoundException/NoClassDefFoundError from incompatible Morphe patch bundles so they fail per-source only
